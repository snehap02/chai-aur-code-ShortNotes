const id = 2; //blocked scope variable //cannot change its value
let username = "snehap02"; //blocked scope variable //can change its value sometimes
var password = "123456"; //global scope variable //can always change its value.
let state;

console.table([id, username, password, state]);